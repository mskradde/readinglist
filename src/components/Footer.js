import React from "react";
import styled from "@emotion/styled";

const Footer = styled.footer`
  width: 100vw;
  display: flex;
  flex-wrap: wrap;
  background: black;
`;
function setFooter(props) {
  return (
    <>
      <Footer></Footer>
    </>
  );
}

export default setFooter;
